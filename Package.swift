// swift-tools-version:5.0

import PackageDescription

let package = Package(
	name: "TraitCollection",
	products: [
		.library(
			name: "TraitCollection",
			targets: ["TraitCollection"]),
	],
	targets: [
		.target(
			name: "TraitCollection",
			dependencies: []),
	]
)
